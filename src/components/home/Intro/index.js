import React from "react"

import Logo from "../../Logo"

import "./index.module.css"

const Intro = () => (
  <div styleName="root">
    <div styleName="logo">
      <Logo color="darkBlue" />
    </div>
  </div>
)

export default Intro
